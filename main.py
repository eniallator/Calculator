"""The main calculator class"""
# First get tokens, in the form of a multidimensional array
# Then check if it makes sense (i.e term followed by an operator then a term etc.)
# Then put tokens into instances of the term class but keep operators
# For every new dimension to the array, put all terms/operators inside a brackets instance
# Brackets will be evaluated first


def main():
    """Where everything is put together"""
    pass

if __name__ == '__main__':
    main()
